# Opt out of pnpm v11's minimumReleaseAge supply-chain policy.
# This policy rejects packages published within the last 24 hours.
# For a single-developer portfolio project the security trade-off is accepted;
# re-evaluate if this ever becomes a shared/org package.
minimum-release-age=0
